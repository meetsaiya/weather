export default function App() {
  return (
    <main className="min-h-screen flex items-center justify-center p-6">
      <div className="max-w-md text-center">
        <h1 className="text-3xl font-semibold text-sky-400">WeatherWise</h1>
        <p className="mt-3 text-slate-300">
          Session 1 scaffold. Open the browser console and call{' '}
          <code className="text-sky-300">testWeatherSlice()</code> to verify the weather engine.
        </p>
      </div>
    </main>
  );
}
